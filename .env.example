# =====================================================
# OpenIntraHub Configuration Template
# Copy this file to .env and fill in your values
# OR use the Web Installer: http://localhost:3000/setup
# =====================================================

# Application
NODE_ENV=production
PORT=3000
FRONTEND_URL=http://localhost:3000

# JWT Authentication
JWT_SECRET=CHANGE_ME_GENERATE_WITH_OPENSSL_RAND_HEX_64
JWT_EXPIRES_IN=24h

# PostgreSQL Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=openintrahub
DB_USER=postgres
DB_PASSWORD=CHANGE_ME
DB_SSL=false

# Redis Cache (Optional)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# LDAP/Active Directory (Optional)
LDAP_ENABLED=false
LDAP_URL=ldap://your-ad-server.local:389
LDAP_BIND_DN=CN=Service Account,OU=Users,DC=example,DC=local
LDAP_BIND_PASSWORD=
LDAP_SEARCH_BASE=DC=example,DC=local
LDAP_SEARCH_FILTER=(sAMAccountName={{username}})

# Email / SMTP (Optional)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@openintrahub.local

# Logging
LOG_LEVEL=info
