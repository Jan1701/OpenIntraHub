# =====================================================
# OpenIntraHub v0.1.1-alpha - Production Environment
# =====================================================

# =====================================================
# APPLICATION
# =====================================================
NODE_ENV=production
PORT=3000
LOG_LEVEL=info

# =====================================================
# DATABASE (PostgreSQL)
# =====================================================
DB_HOST=postgres
DB_PORT=5432
DB_NAME=openintrahub
DB_USER=openintrahub
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE

# =====================================================
# REDIS (Chat, Caching, Sessions)
# =====================================================
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=CHANGE_ME_REDIS_PASSWORD_HERE

# =====================================================
# JWT AUTHENTICATION
# =====================================================
# WICHTIG: Generiere einen sicheren Secret mit: openssl rand -hex 64
JWT_SECRET=CHANGE_ME_GENERATE_WITH_OPENSSL_RAND_HEX_64
JWT_EXPIRES_IN=24h

# =====================================================
# LDAP / ACTIVE DIRECTORY (Optional)
# =====================================================
LDAP_ENABLED=false
LDAP_URL=ldap://your-ldap-server.example.com
LDAP_BIND_DN=cn=admin,dc=example,dc=com
LDAP_BIND_PASSWORD=LDAP_ADMIN_PASSWORD
LDAP_SEARCH_BASE=dc=example,dc=com
LDAP_SEARCH_FILTER=(uid={{username}})

# Alternative für Active Directory:
# LDAP_URL=ldap://your-ad-server.example.com:389
# LDAP_BIND_DN=CN=Service Account,OU=Users,DC=example,DC=com
# LDAP_SEARCH_BASE=DC=example,DC=com
# LDAP_SEARCH_FILTER=(sAMAccountName={{username}})

# =====================================================
# FRONTEND / CORS
# =====================================================
# Deine öffentliche URL (wichtig für CORS & Socket.io)
FRONTEND_URL=https://intranet.example.com

# =====================================================
# FILE UPLOADS
# =====================================================
UPLOAD_MAX_SIZE=25mb
UPLOAD_PATH=/app/uploads

# =====================================================
# E-MAIL (für Notifications - später)
# =====================================================
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=noreply@example.com
# SMTP_PASSWORD=SMTP_PASSWORD
# SMTP_FROM=OpenIntraHub <noreply@example.com>

# =====================================================
# SECURITY
# =====================================================
# RATE_LIMIT_ENABLED=true
# RATE_LIMIT_MAX_REQUESTS=100
# RATE_LIMIT_WINDOW_MS=900000

# =====================================================
# ONLYOFFICE INTEGRATION (Optional)
# =====================================================
# ONLYOFFICE_ENABLED=false
# ONLYOFFICE_URL=https://office.example.com
# ONLYOFFICE_SECRET=ONLYOFFICE_JWT_SECRET

# =====================================================
# JITSI INTEGRATION (Optional)
# =====================================================
# JITSI_ENABLED=false
# JITSI_DOMAIN=meet.jit.si
# JITSI_APP_ID=your_app_id

# =====================================================
# MONITORING (Optional)
# =====================================================
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
# PROMETHEUS_ENABLED=false
